# Environment variables
.env

# Python specific ignores
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/                 # Virtual environment folder (often named 'env' or 'venv')
venv/                # Another common virtual environment folder name
.ipynb_checkpoints/  # Jupyter Notebook checkpoints
.pytest_cache/       # pytest cache directory
.coverage            # Coverage.py results file
.mypy_cache/         # MyPy type checker cache
.ruff_cache/         # Ruff linter/formatter cache

# IDE-specific files (e.g., VS Code, PyCharm)
.vscode/
.idea/

# Operating System generated files
.DS_Store            # macOS
Thumbs.db            # Windows

# Build artifacts and distributions
dist/
build/
*.egg-info/

# Log files and temporary data
*.log
*.tmp
*.bak

# Qdrant Database specific ignores (since you have a qdrant_db folder)
# Assuming you don't want to track the actual Qdrant data files,
# only the structure or relevant config if any.
# If you want to track some specific Qdrant config, adjust these lines.
qdrant_db/
# You might want to ignore the whole qdrant_db/ if it's purely generated data
# qdrant_db/